{
  "name": "nodetest",
  "version": "1.0.0",
  "description": "Testing to see if we can communicate with Deployment via node in a... better way.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "amqplib": "^0.10.3",
    "dotenv": "^16.3.1",
    "prompt-sync": "^4.2.0"
  }
}
